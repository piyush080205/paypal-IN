kind = "web"
previewPath = "/"
title = "PayPal Clone"
version = "1.0.0"
id = "artifacts/paypal-clone"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19111

[services.development]
run = "pnpm --filter @workspace/paypal-clone run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/paypal-clone", "run", "build" ]
publicDir = "artifacts/paypal-clone/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19111"
BASE_PATH = "/"
